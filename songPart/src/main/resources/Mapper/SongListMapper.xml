<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.songPart.dao.SongListMapper">
    <resultMap id="BaseResultMap" type="com.java.songPart.domain.SongList" >
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="pic" jdbcType="VARCHAR" property="pic"/>
        <result column="style" jdbcType="VARCHAR" property="style"/>
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="visible" jdbcType="INTEGER" property="visible"/>
    </resultMap>

    <resultMap id="ResultMapWithBLOBs" type="com.java.songPart.domain.SongList" extends="BaseResultMap">
        <result column="introduction" jdbcType="LONGVARCHAR" property="introduction"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,title,pic,style,user_id,visible
    </sql>

    <sql id="Blob_Column_List">
        id,title,pic,style,introduction,user_id,visible
    </sql>

    <insert id="insert" parameterType="com.java.songPart.domain.SongList">
        insert into song_list
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="title != null">
                title,
            </if>
            <if test="pic != null">
                pic,
            </if>
            <if test="introduction != null">
                introduction,
            </if>
            <if test="style != null">
                style,
            </if>
            <if test="userId != null">
                user_id,
            </if>
            visible,
        </trim>
        <trim prefix=" values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id},
            </if>
            <if test="title != null">
                #{title},
            </if>
            <if test="pic != null">
                #{pic},
            </if>
            <if test="introduction != null">
                #{introduction},
            </if>
            <if test="style != null">
                #{style},
            </if>
            <if test="userId != null">
                #{userId},
            </if>
            1,
        </trim>
    </insert>

    <update id="update" parameterType="com.java.songPart.domain.SongList">
        update song_list
        <set>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="pic != null">
                pic = #{pic},
            </if>
            <if test="introduction != null">
                introduction = #{introduction},
            </if>
            <if test="style != null">
                style = #{style},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="visible != null">
                visible = #{visible},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Integer">
        delete from song_list
        where id=#{id}
    </delete>

    <select id="selectByPrimaryKey" resultMap="ResultMapWithBLOBs" parameterType="java.lang.Integer">
        select
        <include refid="Blob_Column_List"/>
        from song_list
        where id=#{id}
    </select>

    <select id="selectByUserId" resultMap="ResultMapWithBLOBs" parameterType="java.lang.Integer">
        select
        <include refid="Blob_Column_List"/>
        from song_list
        where user_id=#{userId}
    </select>

    <select id="allSongList" resultMap="ResultMapWithBLOBs">
        select
        <include refid="Blob_Column_List"/>
        from song_list
        where visible = 1
    </select>

    <select id="songListOfTitle" resultMap="ResultMapWithBLOBs" parameterType="java.lang.String">
        select
        <include refid="Blob_Column_List"/>
        from song_list
        where title = #{title}
        and visible = 1
    </select>

    <select id="likeTitle" resultMap="ResultMapWithBLOBs" parameterType="java.lang.String">
        select
        <include refid="Blob_Column_List"/>
        from song_list
        where title like #{title}
        and visible = 1
    </select>

    <select id="likeStyle" resultMap="ResultMapWithBLOBs" parameterType="java.lang.String">
        select
        <include refid="Blob_Column_List"/>
        from song_list
        where style like #{style}
        and visible = 1
    </select>

    <select id="likeOtherStyle" resultMap="ResultMapWithBLOBs">
        select
        <include refid="Blob_Column_List"/>
        from song_list
        where `style` not like '%摇滚%'
        and `style` not like '%流行%'
        and `style` not like '%说唱%'
        and `style` not like '%金属%'
        and `style` not like '%民谣%'
        and `style` not like '%电子%'
        and `style` not like '%古典%'
        and `style` not like '%ACG%'
        and visible = 1
    </select>

    <select id="allInvisible" resultMap="ResultMapWithBLOBs">
        select
        <include refid="Blob_Column_List"/>
        from song_list
        where visible = 0
    </select>
</mapper>
















